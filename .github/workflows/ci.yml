name: React Native CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:

  # =========================
  # TEST + LINT + TYPESCRIPT
  # =========================
  test:
    name: Test, Lint and TypeScript
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 
        # GitHub repository નો code GitHub Actions runner પર લાવે છે.

      - name: Setup Node.js
        uses: actions/setup-node@v4
        # GitHub Actions runner પર Node.js environment set up કરે છે.
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test:ci

      - name: Run lint
        run: npm run lint

      - name: TypeScript check
        run: npm run typecheck


  # =========================
  # ANDROID AAB BUILD
  # =========================
  android:
    name: Android Release AAB
    runs-on: ubuntu-latest

    needs:
      - test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Setup Java
        uses: actions/setup-java@v4
        # GitHub Actions runner પર Java environment set up કરે છે.
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Android
        uses: android-actions/setup-android@v3
        # GitHub Actions runner પર Android environment set up કરે છે.

      - name: Make Gradle wrapper executable
        working-directory: android
        run: chmod +x gradlew
        # GitHub Actions runner પર gradlew file ને executable બનાવે છે.

      - name:  Build Android Debug APK
        working-directory: android
        run: ./gradlew assembleDebug

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        # GitHub Actions runner પર build થયેલ APK file ને upload કરે છે.
        with:
          name: QueryDemo-release-aab
          path: android/app/build/outputs/apk/debug/app-debug.apk


# =========================
# iOS CI/CD - HIDDEN FOR NOW
# =========================
#
#  ios:
#    name: iOS Build
#    runs-on: macos-latest
#    needs:
#      - test
#
#    steps:
#      - uses: actions/checkout@v4
#
#      - uses: actions/setup-node@v4
#        with:
#          node-version: 22
#          cache: npm
#
#      - run: npm ci
#
#      - name: Install CocoaPods
#        working-directory: ios
#        run: pod install
#
#      - name: Build iOS
#        working-directory: ios
#        run: |
#          xcodebuild \
#            -workspace QueryDemo.xcworkspace \
#            -scheme QueryDemo \
#            -configuration Debug \
#            -sdk iphonesimulator \
#            -destination 'generic/platform=iOS Simulator' \
#            CODE_SIGNING_ALLOWED=NO \
#            CODE_SIGNING_REQUIRED=NO \
#            build